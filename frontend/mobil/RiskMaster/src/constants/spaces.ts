import {SizeHelper} from '../utility/helpers/sizeHelper'

export const spaces = {
  contentHorizontal: SizeHelper.normalize(16),
  contentVertical: SizeHelper.normalize(24),
  small: SizeHelper.normalize(5),
  normal: SizeHelper.normalize(10),
  medium: SizeHelper.normalize(15),
  big: SizeHelper.normalize(20),
  large: SizeHelper.normalize(25),
  extraLarge: SizeHelper.normalize(30),
}
